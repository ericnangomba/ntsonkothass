[build]
  command = "pnpm install && pnpm run build"
  publish = "artifacts/ntsonkotha-website/dist/public"

[build.environment]
  NODE_ENV = "production"
  BASE_PATH = "/"
  PORT = "3000"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true
